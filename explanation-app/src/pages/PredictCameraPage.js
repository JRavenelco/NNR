import React from 'react';
import './PageStyles.css';

function PredictCameraPage() {
  return (
    <div className="page-content">
      <h2>Predicción en Tiempo Real con Cámara (<code>predict_camera.py</code>)</h2>
      <p>
        El script <code>predict_camera.py</code> utiliza el modelo entrenado para realizar clasificaciones de objetos 
        en tiempo real utilizando la cámara web del ordenador.
      </p>
      <p><em>Contenido detallado para esta sección se añadirá próximamente...</em></p>
    </div>
  );
}

export default PredictCameraPage;
