import React from 'react';
import './PageStyles.css';

function TrainPage() {
  return (
    <div className="page-content">
      <h2>Proceso de Entrenamiento (<code>train.py</code>)</h2>
      <p>
        El script <code>train.py</code> orquesta todo el proceso de entrenamiento del modelo CNN. 
        Aquí es donde el modelo aprende de los datos.
      </p>
      <p><em>Contenido detallado para esta sección se añadirá próximamente...</em></p>
    </div>
  );
}

export default TrainPage;
